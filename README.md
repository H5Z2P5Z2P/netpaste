# NetPaste

一个极简风格的开源网络剪贴板，支持密码保护和阅后即焚。基于 FastAPI + Redis 构建。

## 功能特点
- 🚀 **极简设计**: 无需登录，即开即用
- ⚡ **实时保存**: 内容修改自动同步
- 🔒 **安全保护**: 支持设置访问密码
- ⏱️ **阅后即焚**: 可设置过期时间 (1小时 ~ 30天)
- 📱 **多端同步**: 适配移动端和桌面端

## 快速开始

### 依赖环境
- Python 3.9+
- Redis Server

### 安装启动
1. **克隆项目**
   ```bash
   git clone https://github.com/H5Z2P5Z2P/netpaste.git
   cd netpaste
   ```

2. **安装依赖**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

3. **启动 Redis**
   确保本地 Redis服务已启动 (默认端口 6379)

4. **运行服务**
   ```bash
   # 在 backend 目录下
   uvicorn app.main:app --host 0.0.0.0 --port 8000
   ```

### Docker 部署 (推荐)
```bash
docker-compose up -d --build
```

5. **访问**
   打开浏览器访问 `http://localhost:8000`

## 许可证
MIT License